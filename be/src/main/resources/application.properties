spring.application.name=PBL3
server.port=8080
server.address= 0.0.0.0

# Database Config

spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ecommerce_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=Pranpriyahan2001


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP connection pool

spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=180000
spring.datasource.hikari.pool-name=Ecommerce

# JPA/Hibernate Config

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
logging.level.com.example.PBL3=DEBUG

# Spring security

#spring.security.user.roles=ADMIN

# Thymeleaf config

spring.thymeleaf.enabled=true
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# File upload configuration
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Static resources
spring.web.resources.static-locations=classpath:/static/,file:${file.upload-dir}

# Debug config

logging.level.org.springframework.security = DEBUG

# Spring security
#spring.security.user.name=admin
#spring.security.user.password=123456

spring.jackson.serialization.indent-output=true

# File upload configuration
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB